# MapleStory N — F/P Poison Region 機制規格

毒池生成、蔓延、引爆的完整量化模型。所有數字由影片逐幀實測推導。

此文件為**實作規格**，自包含、可獨立使用。

| | |
|---|---|
| 版本 | 2026-07-26 |
| 資料來源 | 4 支錄影，共 6749 幀 |
| 主要驗證 | 訓練場 91 次毒爆 + 110 次 Flame Sweep；2 支無攻擊對照片；1 支補毒對照片 |

---

## 1. 核心模型

```
全域判定網格：每 0.922 秒一次
```

同一個時鐘管兩件事，兩者由**獨立的對照實驗**各自量到，數值一致：

| 用途 | 量測來源 | 結果 |
|---|---|---|
| 毒素向外蔓延一階 | 2 支無攻擊影片、5 個間隔 | 0.920 s |
| 被引爆的毒池補上新毒 | 1 支補毒影片、4 次恢復 | 0.922 s |

### 完整循環

```
施放 Poison Region
  ↓  1.17 s（技能動畫）
第 1 次判定 → 產生最內側毒池 L1 / R1
  ↓  每 0.922 s 一次判定，向外推進一階
     L2/R2 → L3/R3 → …

每個毒池：
  生成
    ↓  +1.500 s          ← 技能描述，硬性
  可引爆
    ↓  等下一次 Flame Sweep（0.600 s 網格）
  毒爆
    ↓  死區 0.68 s        ← 毒池消失、格位釋出
    ↓  等到下一個 0.922 s 判定
  補上新毒 → 循環
```

### 自洽檢查

| | 模型預測 | 訓練場實測 |
|---|---:|---:|
| 平均週期 | 0.68 + 0.46 + 1.50 + 0.30 = **2.94 s** | **2.89 s** |
| 最短週期 | 0.68 + 0 + 1.50 + 0 = **2.18 s** | **2.283 s** |

---

## 2. 參數表

```
JUDGE_INTERVAL = 0.922 s     全域判定網格（蔓延 + 補毒共用）
ARM_DELAY      = 1.500 s     生成 → 可引爆（技能描述，硬性）
DEAD_TIME      = 0.68  s     毒爆 → 格位釋出（擬合值，±0.05）
DETONATOR_CD   = 0.600 s     Flame Sweep 施放間隔
TRIGGER_LAG    = 0.19  s     施放動畫 → 畫面爆炸（純視覺，不影響次數）
CAST_ANIM      = 1.17  s     施放 → 第 1 次判定
POOL_PITCH     = 285   px    相鄰毒池間距（1920×1080 座標）
```

### 各參數的證據強度

| 參數 | 強度 | 說明 |
|---|---|---|
| `JUDGE_INTERVAL` | ★★★ | 2 種機制、3 支影片、9 個獨立量測，一致 |
| `ARM_DELAY` | ★★★ | 遊戲內技能描述 |
| `DETONATOR_CD` | ★★★ | 110 次施放，中位數 0.600 s、std 40 ms |
| `TRIGGER_LAG` | ★★☆ | R1 池 204 ± 24 ms |
| `CAST_ANIM` | ★☆☆ | 單次量測（僅一支影片抓到綠色光環） |
| `DEAD_TIME` | ★☆☆ | 擬合值；獨立的「毒素歸零」量測為 0.93 s，方向一致 |
| `POOL_PITCH` | ★★☆ | 訓練場 5 個毒池的爆炸位置 |

---

## 3. 演算法

```
輸入:
    pools     : [{name, x, spawn_time}]     初始生成時刻
    events    : 所有引爆技能的施放時刻，合併後排序（可多技能、間隔不必固定）
    duration  : 模擬長度
    grid_phase: 判定網格相位 ψ

主迴圈（每個 pool 獨立）:
    spawn = pool.spawn_time
    loop:
        armed = spawn + ARM_DELAY
        if armed > duration: break

        i = 第一個 events[i] >= armed             # 二分搜尋
        if i 不存在: break
        detonated = events[i]
        visible   = detonated + TRIGGER_LAG
        記錄爆炸(pool, spawn, armed, detonated, visible,
                wait = detonated - armed)

        # 補毒：死區後的第一個判定
        free  = detonated + DEAD_TIME
        k     = ceil((free - ψ) / JUDGE_INTERVAL)
        spawn = ψ + k * JUDGE_INTERVAL
        記錄(respawn_gap = spawn - detonated)      # ← 輸出，不是輸入
```

複雜度 O(池數 × 循環數 × log 事件數)。

### 三個容易寫錯的地方

1. **`respawn_gap` 是輸出不是輸入。** 它等於 `死區 + 到下一個判定的剩餘時間`，
   會在 **0.68 – 1.60 s** 之間浮動，因為毒爆相對判定網格是隨機相位。
   不要寫成固定常數。
2. **判定網格是全域且自走的**，不因毒爆而重置。所有毒池共用同一個 ψ。
3. **一個生成週期只爆一次。** 第一個落在 armed 之後的引爆事件消耗掉該毒池；
   同一週期內後續的引爆事件對這個池無效（對其他池仍有效）。

### 0.4 秒傷害衰減

依 `visible` 排序後，若與前一次爆炸相距 ≤ 0.400 s，該次傷害倍率 **0.40**，否則 **1.00**。
（來自韓版資料，本次未獨立驗證。）

---

## 4. 蔓延

### 4.1 實測時間軸

以施放（角色腳下出現綠色光環）為 t = 0：

```
t = 0        施放 — 綠色光環
t = +0.08    地面出現綠色球體（圖騰成形動畫開始）
t = +0.38    圖騰爆發（白光 + 黑煙環）
t = +0.45    成形動畫結束
             ─────────────────────
t = +1.17    第 1 次蔓延判定
t = +2.09    第 2 次蔓延判定
t = +3.01    第 3 次蔓延判定
t = +3.93    第 4 次蔓延判定
   …         之後每 +0.922 s 一次
```

### 4.2 量測依據

兩支「只放圖騰、全程不攻擊」的對照影片（1920×1080 / 60 fps CFR），
星芒模板全片最高分 0.608（門檻 0.85）→ 確認 0 次引爆：

| 影片 | 地圖 | 判定幀 | 間隔（幀） |
|---|---|---|---|
| `2026-07-26 03-12-12` | 武陵道場大廳 | 161, 217, 271 | 56, 54 |
| `2026-07-26 03-47-07` | 長平雪地圖 | 168, 224, 280, 334 | 56, 56, 54 |

合併 5 個間隔：平均 **0.9200 s**，標準誤 8.2 ms，範圍 0.9000 – 0.9333 s。

觀測只落在 54 幀與 56 幀兩個值，沒有中間值 —— 這是 60 fps 對非 60 分頻週期取樣的
量化特徵。真值最可能是 **0.9167 s（55 幀）**，殘差 ±17 ms 完全對稱。

### 4.3 補毒判定與蔓延判定是同一個時鐘

補毒影片（`2026-07-26 04-07-27`）中，4 次「毒爆 → 新毒出現」的時刻：

| 毒池 x | 毒爆 | 歸零 | 新毒出現 | 間隔 |
|---:|---:|---:|---:|---:|
| 1375 | 9.467 | 10.233 | 11.050 | 1.583 s |
| 1675 | 9.850 | 10.850 | 12.000 | 2.150 s |
| 1100 | 13.083 | 14.217 | 14.767 | 1.683 s |
| 250 | 13.500 | 14.317 | 14.717 | 1.217 s |

四個恢復時刻落在**同一個網格**上：擬合 **P = 0.9220 s，最大殘差 25 ms**。

> 若恢復時刻彼此無關，四次全部落在同一網格 ±25 ms 內的機率為 **0.001%**。

這個 0.9220 s 與 §4.2 獨立量到的 0.9200 s 幾乎相同 —— **同一個時鐘**。

### 4.4 已知範圍

擴張至少持續 4 次判定。長平地圖上毒場推到畫面右緣（距圖騰 +1270 px）才超出視野，
**上限未測得**。左側在該圖被地形擋住（x≈370），非機制限制。

---

## 5. 上限與飽和

```
單一毒池爆炸上限 = 1 / 2.89 s ≈ 0.35 次/秒
```

**引爆得更頻繁不會增加爆炸次數。** 週期由「死區 + 判定網格 + arm」決定，
與引爆頻率無關；提早引爆只會讓下一輪等更久。

唯一會損失的情況是引爆間隔超過可引爆視窗（毒池從 armed 到被下一次生成取代的時間）。
Flame Sweep 的 0.600 s 已有約 2 倍餘裕，實測漏池 0 次。

**結論：追加引爆技能對毒傷沒有幫助，只會佔用施放時間。**

---

## 6. 左右同步 / 錯開

左右毒池的週期略有差異（L1 2.804 s vs R1 2.763 s，差 41 ms/cycle），會緩慢漂移。
當底層偏移累積超過約半個引爆間隔時，兩側就落到不同的 Flame Sweep 上。

實測第二支影片：前 7 輪同幀爆炸，第 8 輪起分岔，之後右池固定領先 0.6 或 1.2 s。

**這對傷害是有利的：**

| 左右關係 | 40% 衰減次數 | 等效滿傷 | 效率 |
|---|---:|---:|---:|
| 週期不同、持續漂移（實測） | 13.8 | 119.7 | **93.5%** |
| 同週期、鎖死同相（最壞） | 65 | 89.6 | **70.0%** |
| 同週期、錯開 ≥ 0.4 s（最佳） | 0 | 129.0 | 100% |

**模擬器若把左右設成完全同步，會低估傷害約三成。務必保留週期差異。**

---

## 7. 驗證用參考數據

以下為 180 s、4 個毒池、Flame Sweep 0.6 s 的模擬結果，用來對答案。

| 情境 | 引爆技能 | 爆炸次數 |
|---|---|---:|
| 1 | 單一 0.600 s | **254** |
| 2 | 0.600 s + 2.5 s | **254** |
| 3 | 1.1 s + 1.7 s + 8.0 s | 253 |

**情境 1 與 2 次數完全相同** —— 多加引爆技能只改變「誰引爆的」，不改變總數。
這是實作正確與否的關鍵測試。

引爆間隔掃描（達成率 = 實際爆炸 / 理論上限）：

| 引爆間隔 | 達成率 |
|---:|---:|
| 0.40 – 1.24 s | ~99% |
| 1.30 s | 95.7% |
| 1.40 s | 84.4% |
| 2.00 s | 62.3% |
| 3.00 s | 39.3% |

3 分鐘、僅計 L1+R1：**約 128 次爆炸**，其中約 14 次吃 0.4 s 衰減，
等效滿傷約 120 次（93.5% 效率）。

---

## 8. 已知限制

1. **`DEAD_TIME = 0.68 s` 是擬合值。** 獨立量到的「毒爆 → 毒素完全歸零」平均 0.93 s
   （4 次：0.766 / 1.000 / 1.134 / 0.817），畫面淡出晚於邏輯消失，方向一致但未直接驗證。
2. **`CAST_ANIM = 1.17 s` 只有單次量測。** 僅道場那支影片抓到綠色光環；長平地圖那支
   施放在錄影起點附近，無法複驗。
3. **0.4 秒衰減規則未獨立驗證**，沿用韓版資料。
4. **外圈毒池（L3+、R3+）的補毒延遲較長**（雪地片量到 1.22–2.15 s，內圈約 0.5 s），
   可能是蔓延要重新推出去。模擬器若只算 L1/R1 可忽略。
5. 全部數據來自單一角色、單一技能循環。換裝備 / 攻速後是否改變未驗證 ——
   但這些看起來是技能本身的屬性。
6. 本模型逐次預測命中率 **72.5%**（訓練場 91 次爆炸）。
   對照「每池自走時鐘」的經驗模型為 82.4%，但那個模型缺乏物理意義。
   平均週期只差 1.7%，最短週期也對得上，對 DPS 模擬足夠。

---

## 9. 快速實作檢查表

- [ ] 判定網格是全域自走的 `ψ + n × 0.922`，**沒有**任何地方因毒爆而重置
- [ ] 補毒 = 死區之後的第一個判定，不是固定延遲
- [ ] `respawn_gap` 是計算出來的輸出欄位
- [ ] 一個生成週期只取第一個引爆事件
- [ ] 引爆事件是合併排序後的清單，支援多技能與不規則間隔
- [ ] 左右毒池保留 41 ms/cycle 的週期差
- [ ] 情境 1 與情境 2 跑出來的爆炸次數**相同**（254）
- [ ] 引爆間隔 2.0 s 時達成率掉到約 62%

---

## 10. 詳細分析與原始資料

完整的量測過程、驗證圖片、CSV 與圖表存於 `analysis/`：

| 路徑 | 內容 |
|---|---|
| `analysis/README.md` | 第一支影片（VFR）的分析 |
| `analysis/v2/README.md` | 第二支影片（60fps）的完整分析 |
| `analysis/v2/frames_spread/` | 道場片：施放與 3 次蔓延判定的前後幀 |
| `analysis/v2/frames_flatmap/` | 雪地片：4 次蔓延判定的前後幀 |
| `analysis/v2/frames_respawn/` | 補毒片：毒爆 → 歸零 → 新毒出現的序列 |
| `analysis/v2/pool_clock_timeline.png` | 4 個毒池的時鐘時間軸 |
| `analysis/v2/spread_steps*.png` | 蔓延跳階圖 |
| `analysis/v2/*.csv` | 全部爆炸事件、生命週期、幀↔時間對照表 |
